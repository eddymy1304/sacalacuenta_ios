//
//  BottomBar.swift
//  sacalacuenta
//
//  Created by Eddy Mendoza on 11/12/24.
//

import SwiftUI

struct BottomBar: View {
    
    var body: some View {
        VStack{}
        /*
        TabView {
            ForEach(screens, id: \.id) { screen in
                
                let text = String(format: NSLocalizedString(screen.title, comment: ""))
                
                Text(text)
                
                    .tabItem {
                        Label(
                            text,
                            systemImage: screen.icon
                        )
                    }
            }
        }*/
    }
}

#Preview {
    BottomBar()
}
